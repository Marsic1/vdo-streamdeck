# Stream Deck review — 2026-09-05

## Follow-up: icon update ordering

Reproduced an overlapping icon update restoring the old command's on-image after a newer command had painted both states. An old failed update also removed the newer command's cache entry. Check the current cached identity before continuing or clearing an older update. Existing images, user overrides, and transport payloads are unchanged. Two deterministic regression tests reproduce the ordering and failure cases in `plugin/tests/command-icon-order.test.ts`.

The complete suite now passes 142 tests across 19 files. Both Elgato USB devices remain detected; Stream Deck and OBS processes are running. The installed plugin is still 0.1.12.0, so these source/build results do not establish that the connected hardware is running the new fixes.

## Follow-up: selected guest changes

Reproduced a pending hangup confirmation carrying from selected guest A to selected guest B. Clear pending guest confirmations in the existing selected-target subscription. Unchanged selection still executes after two presses; changed selection requires fresh confirmation and then targets B correctly. Two regression tests cover both paths. The complete suite now passes 140 tests across 18 files; TypeScript, build, and bundled runtime integration pass.

OBS was running during this follow-up. A read-only WebSocket handshake on its observed local port reported obs-websocket 5.7.4 and required authentication; no control requests or authentication changes were made. The other observed OBS endpoint was unavailable when checked. Native UI inspection still failed with the unavailable computer-use pipe, and the installed Stream Deck plugin remained 0.1.12.0. This does not establish physical-button or OBS end-to-end validation of the new build.

## Confirmed issue and minimal fix

Pending two-press confirmations survived settings updates for Local Control, Guest Command, and Mixer Control. Guest and mixer confirmations also survived leaving and returning to a profile within the confirmation window. Local Control already cleared its confirmation on disappearance.

Clear the existing `armedUntil` entry on settings updates and add the same disappearance cleanup to guest and mixer actions. No command payloads or confirmation durations change. Nine regression cases cover normal two-press execution, settings updates, and disappearance; five reproduced failures before the fix.

Manual check using a disposable test session: press a guarded Hangup or Transfer key once, edit its settings or leave and return to the profile, then press it once again. It must ask for another press, rather than execute immediately. Two ordinary consecutive presses should still execute. Do not run this check against a working production room.

## Validation

- 138 tests pass across 17 files, including existing momentary microphone, dial, feedback, transport, command-registry, and icon tests.
- TypeScript check and build pass.
- Bundled runtime integration passes with the official SDK, simulated Stream Deck events, and a local API endpoint. It exercises every action type, key/dial events, polling, settings persistence, and feedback.
- Elgato CLI 1.7.4 manifest validation and package dry run pass.

## Installed runtime and hardware

- Windows reports two connected Elgato USB devices (vendor 0FD9; product IDs 0060 and 0084).
- Started the existing Stream Deck 7.5.0.22885 application. Its log reports both devices connected and `ninja.vdo.streamdeck` connected to the host.
- Installed VDO.Ninja plugin version is 0.1.12.0. Reviewed/built source is 0.1.13.0. The installed plugin and user profiles were not replaced or edited.
- OBS was not running during inspection. This plugin targets VDO.Ninja's API, including VDO.Ninja used with OBS; it does not directly control OBS's native scene/recording API.
- Windows computer-use failed to connect to its native pipe. Physical key presses, actual key artwork, and OBS end-to-end operation therefore remain unverified. Host registration and device detection do not prove that a VDO.Ninja page is reachable.
